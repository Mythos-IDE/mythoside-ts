# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cargo workspace build output (root Cargo.toml, member src-tauri)
/target

# Sidecar binary, copied in by scripts/prepare-sidecar.mjs before every
# `tauri dev`/`tauri build` — platform-specific, never committed.
/src-tauri/binaries

# scripts/prepare-sidecar.mjs's own throwaway build checkout of
# github.com/Mythos-IDE/mythoside-core — not this repo's source.
/.mythoside-core-src

# Editor directories and files
.vscode
.vite-hooks
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
